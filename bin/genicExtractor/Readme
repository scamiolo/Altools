genicExtractor.cpp

This script will check whether the submitted areas contain some functional genes.

Syntax:

./genicExtractor inputfile annotationFile

Arguments:

inputfile: it is a genic extractor files that is generated by other Altools scripts (e.g. coverageAnalyzer). The file can also be generated by the users since its tab delimited format is very easy to achieve

annotationfile: it is a gff3 format annotation file for the organism under investigation

